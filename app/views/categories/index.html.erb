<h2>Categories Index</h2>

<% @categories.each do |category| %>
  <ul>
  <li>category name: <%= link_to "#{category.name}", category_path(category.id) %></li>
  </ul>  
<% end %>

<%= link_to "Add a Category", new_category_path %>
